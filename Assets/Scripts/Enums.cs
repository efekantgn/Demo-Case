
namespace Enums
{
    public enum SeedState { None, Growing, GrewUp }
    public enum InventorySlotType { None, Drop, Bucket, Sell }
    public enum ItemType { None, Seed, Tool, Material }
    public enum ShopType { None, Buy, Sell }
    public enum QuestType { None, Tutorial, Repeatable }
}
